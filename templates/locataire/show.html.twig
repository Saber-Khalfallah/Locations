{% extends 'layout/base.html.twig' %}

{% block title %}appartement{% endblock %}

{% block body %}
<div class="container">
    <h1>Locataire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdLoc</th>
                <td>{{ locataire.IdLoc }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ locataire.Nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ locataire.Prenom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_locataire_index') }}">back to list</a>

    <a href="{{ path('app_locataire_edit', {'IdLoc': locataire.IdLoc}) }}">edit</a>

    {{ include('locataire/_delete_form.html.twig') }}

</div>
{% endblock %}
